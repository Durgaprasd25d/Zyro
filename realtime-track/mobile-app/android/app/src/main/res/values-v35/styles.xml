<resources xmlns:tools="http://schemas.android.com/tools">
  <!--
    Android 15 (API 35) forces edge-to-edge on all apps.
    The android:statusBarColor and android:navigationBarColor attributes are
    deprecated and ignored from API 35+. React Native's <StatusBar> component
    controls bar colors at runtime, so we only need to opt-in here.
  -->
  <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
    <item name="android:enforceNavigationBarContrast" tools:targetApi="29">false</item>
    <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
    <item name="colorPrimary">@color/colorPrimary</item>
    <item name="android:windowLightStatusBar">true</item>
    <!-- Opt in to edge-to-edge properly (do NOT use statusBarColor / navigationBarColor) -->
    <item name="android:navigationBarColor" tools:targetApi="35">@android:color/transparent</item>
  </style>
  <style name="Theme.App.SplashScreen" parent="AppTheme">
    <item name="android:windowBackground">@drawable/ic_launcher_background</item>
  </style>
</resources>
